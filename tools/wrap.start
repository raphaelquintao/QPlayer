(function (root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD.
        define(factory);
    } else {
        root.QPlayer = factory(root.$, root._);
    }
}(this, function ($, _) {

